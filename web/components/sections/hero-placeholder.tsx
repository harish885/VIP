/**
 * @deprecated Phase 01 placeholder — superseded by `Hero` in
 * `components/marketing/scenes/hero.tsx`. Safe to delete this file
 * (and the sibling `scene-placeholder.tsx`); no remaining imports.
 *
 *   rm -rf web/components/sections
 */
export {};
