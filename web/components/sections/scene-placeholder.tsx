/**
 * @deprecated Phase 01 placeholder — superseded by the real scenes in
 * `components/marketing/scenes/*` plus the reusable `SceneHeader` in
 * `components/marketing/scene-header.tsx`. Safe to delete.
 *
 *   rm -rf web/components/sections
 */
export {};
