/**
 * vip-ui — primitive surface for the Value Intelligence Studio.
 *
 * A tiny in-repo design system: no external dependency, no abstraction
 * beyond what's reused across at least two screens. Every primitive is
 * a thin Tailwind composition that other components import instead of
 * re-implementing the same border + radius + shadow recipe.
 */
export { Button } from './button';
export { StatCell } from './stat-cell';
export { SectionHeader } from './section-header';
export { StatusBadge } from './status-badge';
export { SourceBadge } from './source-badge';
export { EmptyState } from './empty-state';
export { Surface } from './surface';
export { Segmented } from './segmented';
